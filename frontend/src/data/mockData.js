/**
 * mockData.js — Archivo de datos de prueba
 *
 * NOTA: Este archivo ya no se utiliza. Todos los datos provienen
 * directamente del backend (API REST en /api/*).
 *
 * Se conserva el archivo para evitar errores en importaciones
 * existentes, pero todos los exports estan vacios.
 */

export const usuarios      = [];
export const departamentos = [];
export const empleados     = [];
export const asistencia    = [];
export const reportes      = [];

export default {};